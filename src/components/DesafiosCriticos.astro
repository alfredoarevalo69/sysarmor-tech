---
// src/components/DesafiosCriticos.astro

const desafios = [
  { title: "Sistemas Legados", desc: "Plataformas fuera de soporte (EOL) que comprometen la estabilidad." },
  { title: "Brechas de Seguridad", desc: "Falta de estrategia de hardening (ASR/Defender) que deja data expuesta." },
  { title: "Ineficiencia Operativa", desc: "Procesos manuales y gestión de identidades desordenada que drenan el presupuesto." },
  { title: "Falta de Compliance", desc: "Deficiencia en la aplicación de marcos de seguridad y endurecimiento exigidos por normativas." }
];
---

<!-- min-h-[calc(100vh-76px)] y centrado vertical exacto para escritorio, con flujo seguro en móvil -->
<section class="w-full min-h-[calc(100vh-76px)] py-12 px-4 sm:px-6 bg-zinc-200 border-y border-zinc-300 flex flex-col justify-center items-center">
  <div class="max-w-6xl mx-auto w-full my-auto space-y-10">
    
    <!-- Encabezado -->
    <div class="text-center space-y-2">
      <h2 class="text-2xl md:text-3xl font-display font-bold text-neutral-900 tracking-wide uppercase">
        ¿Su infraestructura está limitando su crecimiento?
      </h2>
      <p class="text-zinc-700 font-medium text-base font-sans max-w-2xl mx-auto">
        Si su equipo enfrenta estos escenarios, necesita una intervención de Nivel 3.
      </p>
    </div>
    
    <!-- Grid de Desafíos -->
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
      {desafios.map(item => (
        <div class="bg-white p-6 md:p-8 rounded-xl border-t-4 border-amber-600 border-x-2 border-b-2 border-zinc-300 shadow-md shadow-zinc-400/20 hover:shadow-lg transition-all flex flex-col justify-between">
          <div>
            <h3 class="text-lg font-display font-bold text-amber-700 mb-3">
              {item.title}
            </h3>
            <p class="text-sm text-zinc-700 leading-relaxed font-sans">
              {item.desc}
            </p>
          </div>
        </div>
      ))}
    </div>

  </div>
</section>