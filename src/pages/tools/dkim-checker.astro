---
// src/pages/tools/dkim-checker.astro
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Validador de Registro DKIM | SysArmor Tech">
  <main class="w-full min-h-[calc(100vh-76px)] py-12 px-4 sm:px-6 bg-neutral-100 border-t border-neutral-300 text-neutral-900">
    <div class="max-w-3xl mx-auto space-y-8">
      
      <div>
        <a 
          href="/tools" 
          class="inline-flex items-center gap-2 px-4 py-2 bg-slate-200/85 hover:bg-slate-300 border border-slate-300 rounded-lg text-slate-800 font-bold text-xs uppercase tracking-wider transition-colors shadow-xs"
        >
          <span>←</span> Volver a Herramientas
        </a>
      </div>

      <div class="space-y-2 text-center max-w-xl mx-auto">
        <h1 class="text-3xl md:text-4xl font-display font-bold text-neutral-900 tracking-wide">
          Validador de Registro DKIM
        </h1>
        <p class="text-neutral-700 font-medium text-base">
          Comprueba la clave pública y la integridad de los registros DKIM bajo selectores personalizados para asegurar el correo saliente.
        </p>
      </div>

      <div class="bg-white border-2 border-slate-300 rounded-xl p-6 sm:p-8 shadow-xl space-y-6">
        <form id="dkim-form" class="space-y-4">
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
            <div class="sm:col-span-2 space-y-1">
              <label for="domain" class="block text-xs font-bold text-neutral-700 uppercase tracking-wide">Dominio</label>
              <input
                type="text"
                id="domain"
                required
                placeholder="ej. bancofalabella.com"
                class="w-full bg-neutral-50 border-2 border-slate-300 rounded-lg px-4 py-3 text-neutral-900 font-medium text-sm focus:outline-none focus:border-blue-700 transition-colors"
              />
            </div>
            <div class="space-y-1">
              <label for="selector" class="block text-xs font-bold text-neutral-700 uppercase tracking-wide">Selector</label>
              <input
                type="text"
                id="selector"
                required
                placeholder="ej. 200608, mailjet"
                class="w-full bg-neutral-50 border-2 border-slate-300 rounded-lg px-4 py-3 text-neutral-900 font-medium text-sm focus:outline-none focus:border-blue-700 transition-colors"
              />
            </div>
          </div>
          <button
            type="submit"
            id="submit-btn"
            class="w-full bg-slate-900 hover:bg-slate-800 text-white font-bold px-6 py-3 rounded-lg transition-colors text-sm shadow-sm cursor-pointer h-[46px]"
          >
            Verificar DKIM
          </button>
        </form>
      </div>

      <div id="results" class="hidden bg-white border-2 border-slate-300 rounded-xl p-6 sm:p-8 shadow-xl space-y-6">
        <h2 class="text-xl font-bold text-neutral-900 border-b-2 border-slate-200 pb-3">
          Resultado del Análisis
        </h2>
        
        <div id="results-content" class="space-y-6">
          <!-- Inyectado por JS -->
        </div>
      </div>

    </div>
  </main>
</Layout>

<script>
  const form = document.getElementById('dkim-form') as HTMLFormElement;
  const resultsDiv = document.getElementById('results');
  const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
  const resultsContent = document.getElementById('results-content') as HTMLDivElement;

  const dkimMeta: Record<string, { name: string; desc: string }> = {
    v: { name: 'Version', desc: 'Identifica el registro como un registro DKIM. Debe ser el primer tag.' },
    k: { name: 'Key Type', desc: 'El tipo de llave criptográfica utilizada por el tag (p).' },
    p: { name: 'Public Key', desc: 'La sintaxis y semántica de este valor antes de ser codificado en base64 son definidas por el tag (k).' },
    h: { name: 'Hash Algorithm', desc: 'Algoritmos de hash aceptados para la firma (ej. sha256).' },
    s: { name: 'Service Type', desc: 'Tipos de servicio restringidos para el uso de esta clave.' },
    t: { name: 'Flags', desc: 'Banderas de prueba o políticas estrictas (ej. y para testing).' }
  };

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const domain = (document.getElementById('domain') as HTMLInputElement).value.trim();
    const selector = (document.getElementById('selector') as HTMLInputElement).value.trim();

    if (!domain || !selector) return;

    if (submitBtn) {
      submitBtn.textContent = 'Consultando Servidores DNS...';
      submitBtn.setAttribute('disabled', 'true');
    }
    resultsDiv?.classList.add('hidden');

    try {
      const response = await fetch('/api/check-dkim', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ domain, selector }),
      });

      const data = await response.json();
      resultsDiv?.classList.remove('hidden');

      if (response.ok) {
        if (data.status === 'PASS') {
          // Mapear o traducir descripciones si vienen de la API en inglés
          let rowsHtml = data.tags.map((t: any) => {
            const meta = dkimMeta[t.tag.toLowerCase()] || { name: t.name || t.tag.toUpperCase(), desc: t.description || 'Parámetro personalizado o extendido de DKIM.' };
            return `
              <tr class="border-b border-slate-200 hover:bg-slate-50/50">
                <td class="px-4 py-3 font-mono font-bold text-slate-900 uppercase">${t.tag}</td>
                <td class="px-4 py-3 font-mono text-xs text-blue-700 break-all">${t.value}</td>
                <td class="px-4 py-3 font-semibold text-slate-800">${meta.name}</td>
                <td class="px-4 py-3 text-slate-600 text-xs">${meta.desc}</td>
              </tr>
            `;
          }).join('');

          resultsContent.innerHTML = `
            <div class="p-4 bg-emerald-50 border-emerald-400 text-emerald-900 border-2 rounded-lg space-y-2">
              <div class="flex items-center justify-between">
                <div class="font-bold text-sm uppercase tracking-wide">Estado DKIM: PASS (${data.qname})</div>
              </div>
              <p class="text-xs font-mono break-all bg-white/80 p-3 rounded border border-emerald-200">${data.record}</p>
            </div>

            <div class="overflow-x-auto border border-slate-300 rounded-lg shadow-xs">
              <table class="w-full text-left border-collapse text-sm">
                <thead>
                  <tr class="bg-slate-100 border-b border-slate-300 text-slate-800 font-bold text-xs uppercase tracking-wider">
                    <th class="px-4 py-3">Tag</th>
                    <th class="px-4 py-3">TagValue</th>
                    <th class="px-4 py-3">Name</th>
                    <th class="px-4 py-3">Description</th>
                  </tr>
                </thead>
                <tbody>
                  ${rowsHtml}
                </tbody>
              </table>
            </div>
          `;
        } else {
          resultsContent.innerHTML = `
            <div class="p-4 bg-red-50 border-red-400 text-red-900 border-2 rounded-lg space-y-1">
              <div class="font-bold text-sm uppercase tracking-wide">Consulta: ${data.qname}</div>
              <p class="text-xs">No se halló un registro DKIM válido para el selector provisto. Verifica que el selector y el dominio sean correctos.</p>
            </div>
          `;
        }
      } else {
        resultsContent.innerHTML = `<p class="text-red-700 font-bold text-sm">Error: ${data.error || 'Ocurrió un error en la validación.'}</p>`;
      }
    } catch (err) {
      resultsContent.innerHTML = `<p class="text-red-700 font-bold text-sm">Error de red al conectar con el servidor.</p>`;
    } finally {
      if (submitBtn) {
        submitBtn.textContent = 'Verificar DKIM';
        submitBtn.removeAttribute('disabled');
      }
    }
  });
</script>