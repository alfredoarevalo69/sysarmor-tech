---
// src/pages/tools/what-is-my-ip.astro
import Layout from '../../layouts/Layout.astro';
---

<Layout title="¿Cuál es mi IP y Geolocalización | SysArmor Tech">
  <main class="w-full min-h-[calc(100vh-76px)] py-10 px-4 sm:px-6 bg-slate-100 border-t border-slate-300 text-neutral-900">
    <div class="max-w-3xl mx-auto space-y-6">
      
      <!-- Navegación de retorno -->
      <div>
        <a 
          href="/tools" 
          class="inline-flex items-center gap-2 px-3 py-1.5 bg-amber-400 hover:bg-amber-500 border border-amber-500 rounded-lg text-neutral-900 font-bold text-xs uppercase tracking-wider transition-colors shadow-xs"
        >
          <span>←</span> Volver a Herramientas
        </a>
      </div>

      <!-- Encabezado compacto -->
      <div class="space-y-1 text-center sm:text-left">
        <h1 class="text-2xl sm:text-3xl font-display font-bold text-neutral-900 tracking-wide">
          ¿Cuál es mi IP y Red?
        </h1>
        <p class="text-neutral-600 text-sm">
          Diagnóstico en tiempo real de tus direcciones IP, geolocalización y entorno de conexión.
        </p>
      </div>

      <!-- Tarjeta Principal de IPs (Diseño unificado y estilizado) -->
      <div class="bg-slate-950 border border-slate-800 rounded-2xl p-5 sm:p-6 shadow-xl space-y-4">
        <h2 class="text-xs uppercase tracking-wider text-amber-400 font-bold border-b border-slate-800 pb-2">
          Conectividad de Red
        </h2>
        
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <!-- IPv4 -->
          <div class="bg-slate-900/80 border border-slate-800/80 p-3.5 rounded-xl flex items-center justify-between gap-2">
            <span class="text-xs font-bold uppercase tracking-wider text-slate-400">IPv4 Pública</span>
            <span id="ipv4-display" class="font-mono text-sm sm:text-base font-bold text-amber-400">
              Consultando...
            </span>
          </div>

          <!-- IPv6 -->
          <div class="bg-slate-900/80 border border-slate-800/80 p-3.5 rounded-xl flex items-center justify-between gap-2">
            <span class="text-xs font-bold uppercase tracking-wider text-slate-400">IPv6 Pública</span>
            <span id="ipv6-display" class="font-mono text-xs sm:text-sm font-bold text-slate-200">
              Buscando...
            </span>
          </div>
        </div>
      </div>

      <!-- Panel de Datos Detallados (Formato lista compacta/moderna) -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        
        <!-- Geolocalización -->
        <div class="bg-slate-950 border border-slate-800 rounded-2xl p-5 sm:p-6 shadow-xl space-y-4 flex flex-col justify-between">
          <h2 class="text-xs uppercase tracking-wider text-amber-400 font-bold border-b border-slate-800 pb-2">
            Geolocalización
          </h2>
          
          <ul class="space-y-3 text-sm divide-y divide-slate-900">
            <li class="flex justify-between items-center pt-2">
              <span class="text-slate-400 text-xs font-semibold">País</span>
              <span id="geo-country" class="font-medium text-slate-100 flex items-center gap-1.5">Cargando...</span>
            </li>
            <li class="flex justify-between items-center pt-3">
              <span class="text-slate-400 text-xs font-semibold">Ciudad / Región</span>
              <span id="geo-city" class="font-medium text-slate-100">-</span>
            </li>
            <li class="flex justify-between items-center pt-3">
              <span class="text-slate-400 text-xs font-semibold">ISP / Proveedor</span>
              <span id="geo-isp" class="font-mono text-xs text-slate-300 truncate max-w-[180px] sm:max-w-[220px]" title="-">-</span>
            </li>
            <li class="flex justify-between items-center pt-3">
              <span class="text-slate-400 text-xs font-semibold">Coordenadas</span>
              <span id="geo-coords" class="font-mono text-xs text-slate-300">-</span>
            </li>
          </ul>
        </div>

        <!-- Entorno del Cliente -->
        <div class="bg-slate-950 border border-slate-800 rounded-2xl p-5 sm:p-6 shadow-xl space-y-4 flex flex-col justify-between">
          <h2 class="text-xs uppercase tracking-wider text-amber-400 font-bold border-b border-slate-800 pb-2">
            Entorno del Cliente
          </h2>
          
          <ul class="space-y-3 text-sm divide-y divide-slate-900">
            <li class="flex justify-between items-center pt-2">
              <span class="text-slate-400 text-xs font-semibold">Sistema Operativo</span>
              <span id="client-os" class="font-medium text-slate-100">Detectando...</span>
            </li>
            <li class="flex justify-between items-center pt-3">
              <span class="text-slate-400 text-xs font-semibold">Navegador Web</span>
              <span id="client-browser" class="font-medium text-slate-100">Detectando...</span>
            </li>
            <li class="flex justify-between items-center pt-3">
              <span class="text-slate-400 text-xs font-semibold">Protocolo / Puerto</span>
              <span id="connection-protocol" class="font-mono text-xs text-slate-300">-</span>
            </li>
          </ul>
        </div>

      </div>

      <!-- Botón de actualización sutil -->
      <div class="flex justify-end pt-2">
        <button 
          id="client-refresh"
          class="w-full sm:w-auto bg-slate-900 hover:bg-slate-800 text-amber-400 border border-slate-800 font-bold px-4 py-2.5 rounded-xl transition-colors text-xs uppercase tracking-wider shadow-sm inline-flex items-center justify-center gap-2 cursor-pointer"
        >
          🔄 Actualizar Información
        </button>
      </div>

    </div>
  </main>
</Layout>

<script>
  function detectOSAndBrowser() {
    const ua = navigator.userAgent;
    const osElement = document.getElementById('client-os');
    const browserElement = document.getElementById('client-browser');
    const protoElement = document.getElementById('connection-protocol');

    let os = 'Desconocido';
    if (ua.includes('Win')) os = 'Windows';
    else if (ua.includes('Mac')) os = 'macOS';
    else if (ua.includes('Linux')) os = 'Linux';
    else if (ua.includes('Android')) os = 'Android';
    else if (ua.includes('like Mac') || ua.includes('iOS')) os = 'iOS';
    if (osElement) osElement.textContent = os;

    let browser = 'Desconocido';
    if (ua.includes('Firefox/')) browser = 'Mozilla Firefox';
    else if (ua.includes('Edg/')) browser = 'Microsoft Edge';
    else if (ua.includes('Chrome/')) browser = 'Google Chrome';
    else if (ua.includes('Safari/') && !ua.includes('Chrome')) browser = 'Apple Safari';
    else if (ua.includes('OPR/') || ua.includes('Opera/')) browser = 'Opera';
    if (browserElement) browserElement.textContent = browser;

    if (protoElement) {
      protoElement.textContent = `${window.location.protocol.replace(':', '').toUpperCase()} (${window.location.port || (window.location.protocol === 'https:' ? '443' : '80')})`;
    }
  }

  async function fetchNetworkData() {
    detectOSAndBrowser();
    const ipv4Element = document.getElementById('ipv4-display');
    const ipv6Element = document.getElementById('ipv6-display');
    const countryElement = document.getElementById('geo-country');
    const cityElement = document.getElementById('geo-city');
    const ispElement = document.getElementById('geo-isp');
    const coordsElement = document.getElementById('geo-coords');

    try {
      const resV4 = await fetch('https://api.ipify.org?format=json');
      const dataV4 = await resV4.json();
      if (ipv4Element) ipv4Element.textContent = dataV4.ip;
    } catch {
      if (ipv4Element) ipv4Element.textContent = 'No disponible';
    }

    try {
      const resV6 = await fetch('https://api64.ipify.org?format=json');
      const dataV6 = await resV6.json();
      if (ipv6Element) {
        ipv6Element.textContent = (dataV6.ip && dataV6.ip.includes(':')) ? dataV6.ip : 'No disponible';
      }
    } catch {
      if (ipv6Element) ipv6Element.textContent = 'No disponible';
    }

    try {
      const resGeo = await fetch('https://ipapi.co/json/');
      const geo = await resGeo.json();

      if (!geo.error) {
        if (countryElement) {
          const countryCode = geo.country_code || '';
          const flag = countryCode ? countryCode.toUpperCase().replace(/./g, (char) => String.fromCodePoint(127397 + char.charCodeAt(0))) : '';
          countryElement.textContent = `${flag} ${geo.country_name}`;
        }
        if (cityElement) cityElement.textContent = `${geo.city}, ${geo.region}`;
        if (ispElement) {
          ispElement.textContent = geo.org || 'Desconocido';
          ispElement.setAttribute('title', geo.org || 'Desconocido');
        }
        if (coordsElement) coordsElement.textContent = `${geo.latitude}, ${geo.longitude}`;
      } else {
        throw new Error('Error en geolocalización');
      }
    } catch {
      if (countryElement) countryElement.textContent = 'No disponible';
      if (cityElement) cityElement.textContent = '-';
      if (ispElement) ispElement.textContent = '-';
      if (coordsElement) coordsElement.textContent = '-';
    }
  }

  document.addEventListener('DOMContentLoaded', fetchNetworkData);
  document.addEventListener('astro:page-load', fetchNetworkData);
  document.getElementById('client-refresh')?.addEventListener('click', fetchNetworkData);
</script>